"use strict";  // treat all JS code as newer version;

// alert("Hello") We are using nade js not browsers 

let name = "Divyansh"
let age = 23
let isLoggedIn = false


// Number => 2 to power 53
// bigint
// string => "" ''
// boolean => true/false
// null (stand alone value)
// undefined => value not defined
// symbol => unique
// example of symbole - const id = Symbol('123')


// Object

console.log(typeof"ritansh")
console.log(typeof undefined) //type is undefined
console.log(typeof null) // type is object



// javaScript is dynamically type language:

// Primitive (all by values - change done in copy value not in original vlaue)
// 7 types : String, Number , Boolean, null(not zero its empty), undefined, Symbol(make value Unique),  bigInt.

// Non primitive (Referance - allocated the memory location) 
// Types : Array, Object, Function.

// Array
const heros = ["Shaktimaan", "doga"] 

// Object
let obj = {
    name: "Ritansh",
    age: 23
}

//function
let myfunction = function(){
    console.log("ritansh");
}